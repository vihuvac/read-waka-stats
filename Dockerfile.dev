# Development image: Go toolchain for local runs (bind-mount + go run).
# Production Action image remains Dockerfile.
FROM golang:1.24-alpine3.22

RUN apk --no-cache add ca-certificates git tzdata

WORKDIR /src

COPY go.mod go.sum ./
RUN go mod download

CMD ["go", "run", "./cmd/read-waka-stats"]
